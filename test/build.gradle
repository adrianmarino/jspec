apply plugin: 'ar.com.nonosoft.jspec.spec'

dependencies {
	compile project(':core')
}

buildscript {
	repositories {
		mavenLocal()
	}
	dependencies {
		classpath group: 'ar.com.nonosoft', name: 'jspec-plugin', version: '0.0.1'
	}
}